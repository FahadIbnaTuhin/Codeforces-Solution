#include <bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
	

    return 0;
}

// When it says "side-adjacent lights," it typically refers to the lights directly to the 
// left and right of the light being pressed. Therefore, pressing a light will only affect the lights immediately to its left and right