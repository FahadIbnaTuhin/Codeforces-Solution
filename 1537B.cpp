#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

void solve() {
    ll n, m, i, j; cin >> n >> m >> i >> j;

    // cout << "1 1 " << n << ' ' << m << '\n'; // both correct
    cout << 1 << ' ' << m << ' ' << n << ' ' << 1 << '\n';
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    int t; cin >> t;

    while(t--) {
        solve();
    }

    return 0;
}