#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

int main() {
    // ios::sync_with_stdio(false);
    // cin.tie(0);
    int n, m; cin >> n >> m;

    if (n < m) cout << -1 << '\n';
    else {
        int q = n / 2 + (n % 2) * 
    }

    return 0;
}