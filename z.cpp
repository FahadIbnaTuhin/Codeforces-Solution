#include <iostream>

using namespace std;

int main() {
    // for(size_t i{}; i < 10; ++i) {
    //     cout << i << endl;
    // }

    // int n, k;
    // cin >> n >> k;

    // int arr[n];
    // for(int i = 0; i < n; ++i) {
    //     std::cin >> arr[i];
    // }

    // int count = 0;
    // for(int i : arr) {
    //     cout << i << " " << count << " ";
    //     if (count != k && i >= k) {
    //         cout << count << endl;
    //         count += 1; 
    //     } else {
    //         break;
    //     }
    // }

    // cout << count << endl;

    return 0;
}
